/**
 * Retired.
 *
 * This was the local entry point for the old Slack app. It is kept only so
 * that uploading this project overwrites the previous version's file, which
 * imported Slack packages that are no longer installed.
 *
 * The task tracker needs no local entry point: the site is public/ and the
 * API is api/. Safe to delete whenever you like.
 */
console.log('This project is now a web app. Open the site; there is nothing to run locally.');
